# Local development only. Copy to `.env` (never commit the copy).
# Production values must be set with `wrangler secret put <NAME>` — see SETUP.md.
#
# These keys match wrangler.toml [secrets].required (SUPERADMIN_PASSWORD, SES_*).
# Optional integrations (SENDY_*, GA4_ID) below are for local `wrangler dev` only.

SUPERADMIN_PASSWORD=replace-me-local-only
SES_ACCESS_KEY=replace-me-local-only
SES_SECRET_KEY=replace-me-local-only
SENDY_URL=https://example.invalid/sendy
SENDY_LIST_ID=replace-me-local-only
GA4_ID=G-XXXXXXXXXX
